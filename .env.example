# Optional: Groq API key for cloud fallback (and Japanese polishing).
# Without this, the app still works fully offline with local Whisper.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
